import MainForm from "./components/MainForm";

const App: React.FC = () => {
  return (
    <div className="flex w-full max-h-full items-center justify-center bg-gray-100">
      <MainForm />
    </div>
  );
};

export default App;
